#!/bin/bash

echo "Running pre-commit checks..."

# Run golangci-lint
echo "Running linter..."
make lint
if [ $? -ne 0 ]; then
    echo "❌ Linting failed. Please fix the issues before committing."
    exit 1
fi
